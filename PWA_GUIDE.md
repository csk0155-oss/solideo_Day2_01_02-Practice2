# PWA 설치 및 테스트 가이드 📱

## PWA (Progressive Web App)란?

Progressive Web App은 웹 기술로 만들어진 앱으로, 네이티브 앱처럼 설치하고 사용할 수 있습니다.

### 주요 장점
- 📲 홈 화면에 아이콘으로 추가
- 🚀 빠른 로딩 속도
- 📡 오프라인에서도 작동
- 💾 자동 캐싱
- 🔄 자동 업데이트
- 📱 네이티브 앱과 유사한 UX

## 설치 방법

### Android (Chrome)

1. **Chrome 브라우저에서 앱 접속**
   - http://localhost:8000 또는 배포된 URL 접속

2. **설치 프롬프트 확인**
   - 우측 하단에 "📱 앱으로 설치하기" 버튼이 나타남
   - 또는 주소창 우측에 "+" 아이콘 표시

3. **설치하기**
   - "설치" 버튼 클릭
   - 홈 화면에 앱 아이콘이 자동으로 추가됨

4. **앱 실행**
   - 홈 화면의 앱 아이콘 탭
   - 브라우저 UI 없이 독립 실행됨

### iOS (Safari)

1. **Safari에서 앱 접속**
   - Safari 브라우저 필수 (Chrome 불가)

2. **공유 메뉴 열기**
   - 하단 중앙의 "공유" 버튼(⬆️) 탭

3. **홈 화면에 추가**
   - 스크롤하여 "홈 화면에 추가" 선택
   - 앱 이름 확인/수정
   - "추가" 버튼 탭

4. **앱 실행**
   - 홈 화면의 앱 아이콘 탭
   - 독립 실행됨

### Desktop (Windows/Mac/Linux)

#### Chrome
1. 웹사이트 접속
2. 주소창 우측의 "💻+" 아이콘 클릭
3. "설치" 버튼 클릭
4. 앱이 별도 창으로 열림
5. 작업 표시줄/Dock에 고정 가능

#### Edge
1. 웹사이트 접속
2. 주소창 우측의 설치 아이콘 클릭
3. "설치" 버튼 클릭
4. 시작 메뉴에 추가됨

## 아이콘 생성

### 자동 생성 (권장)

1. **아이콘 생성기 열기**
   ```bash
   # 브라우저에서 열기
   open generate-icons.html
   ```

2. **아이콘 생성**
   - "모든 아이콘 생성" 버튼 클릭
   - 모든 크기의 아이콘이 자동 생성됨

3. **아이콘 저장**
   - 각 아이콘을 마우스 오른쪽 클릭
   - "다른 이름으로 저장" 선택
   - `icons/` 폴더에 저장
   - 파일명: `icon-72x72.png`, `icon-96x96.png`, 등

### 필요한 아이콘 크기
- 72x72
- 96x96
- 128x128
- 144x144
- 152x152
- 192x192
- 384x384
- 512x512

## 테스트 방법

### 1. 로컬 서버 실행

```bash
# Python 3
python3 -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

### 2. Service Worker 확인

Chrome DevTools에서:
1. F12 키를 눌러 개발자 도구 열기
2. "Application" 탭 선택
3. 좌측 "Service Workers" 클릭
4. Service Worker가 등록되어 있는지 확인

### 3. 캐시 확인

1. DevTools > Application > Cache Storage
2. "travel-app-v1" 캐시 확인
3. 캐시된 파일 목록 확인

### 4. 오프라인 테스트

1. DevTools > Network 탭
2. "Offline" 체크박스 선택
3. 페이지 새로고침
4. 오프라인 페이지 또는 캐시된 페이지 표시 확인

### 5. Manifest 확인

1. DevTools > Application > Manifest
2. 앱 이름, 아이콘, 색상 등 확인
3. "Add to home screen" 버튼 클릭하여 테스트

### 6. Lighthouse 감사

1. DevTools > Lighthouse 탭
2. "Progressive Web App" 선택
3. "Analyze page load" 클릭
4. PWA 점수 확인 (목표: 90점 이상)

## 문제 해결

### Service Worker가 등록되지 않음

**원인**: HTTPS 또는 localhost가 아닌 환경
**해결**:
- 로컬 테스트: `localhost` 사용
- 배포: HTTPS 사용

### 설치 버튼이 나타나지 않음

**원인**: PWA 요구사항 미충족
**확인사항**:
- [ ] manifest.json 파일 존재
- [ ] Service Worker 등록됨
- [ ] HTTPS 또는 localhost 사용
- [ ] 모든 아이콘 파일 존재
- [ ] start_url이 유효함

### 오프라인 모드가 작동하지 않음

**원인**: Service Worker 캐싱 실패
**해결**:
1. DevTools에서 Service Worker 상태 확인
2. Cache Storage에 파일이 있는지 확인
3. Service Worker 재등록

### iOS에서 설치가 안됨

**원인**: Safari만 지원됨
**해결**:
- Chrome이 아닌 Safari 사용
- iOS 13 이상 필요
- "홈 화면에 추가" 수동 실행

## PWA 체크리스트

배포 전 확인사항:

- [ ] manifest.json 파일 생성 및 연결
- [ ] Service Worker 구현 및 등록
- [ ] 모든 크기의 아이콘 생성
- [ ] HTTPS 적용 (배포 시)
- [ ] 오프라인 페이지 구현
- [ ] 캐시 전략 구현
- [ ] Meta 태그 추가
- [ ] Lighthouse 테스트 통과 (90점+)

## 배포 가이드

### GitHub Pages 배포

1. **저장소 푸시**
   ```bash
   git add .
   git commit -m "Add PWA features"
   git push origin main
   ```

2. **GitHub Pages 활성화**
   - Settings > Pages
   - Source: main branch
   - Save

3. **HTTPS 확인**
   - GitHub Pages는 자동으로 HTTPS 제공

### Netlify 배포

1. **저장소 연결**
   - Netlify에 로그인
   - "New site from Git" 클릭
   - 저장소 선택

2. **빌드 설정**
   - Build command: (없음)
   - Publish directory: .

3. **배포**
   - "Deploy site" 클릭
   - 자동으로 HTTPS 제공

### Vercel 배포

```bash
# Vercel CLI 설치
npm install -g vercel

# 배포
vercel
```

## 성능 최적화

### 캐시 전략
- **Network First**: 실시간 데이터 (교통 정보)
- **Cache First**: 정적 파일 (CSS, JS, 이미지)
- **Stale While Revalidate**: 자주 변경되는 데이터

### 파일 크기 최소화
- 이미지 최적화
- JavaScript/CSS 압축
- 불필요한 라이브러리 제거

## 추가 기능

향후 추가 가능한 PWA 기능:

- [ ] 백그라운드 동기화
- [ ] 푸시 알림
- [ ] 위치 기반 서비스
- [ ] 오프라인 데이터 저장
- [ ] 앱 업데이트 알림
- [ ] 공유 API 통합

## 참고 자료

- [MDN PWA 가이드](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps)
- [Google PWA 체크리스트](https://web.dev/pwa-checklist/)
- [Can I Use - Service Worker](https://caniuse.com/serviceworkers)

---

**문의사항이나 문제가 있으시면 이슈를 등록해주세요!**
